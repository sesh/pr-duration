[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.ruff]
# Enable Pyflakes and pycodestyle rules.
select = ["E", "F"]
line-length=120

[tool.bandit]
exclude_dirs = ["venv", "build", "pr_duration.egg.info", ".pytest_cache"]
